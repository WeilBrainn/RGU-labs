#include <stdio.h>
#include <stdarg.h>

int polynomial_value(double x, int n, ...)
{
  va_list args;
  va_start(args, n);

  double result = 0.0;
  double term;
  int current_power = n;

  for () {
   double coefficient = va_arg(args, double);
  }
}
int main()
{

  return 0;
}