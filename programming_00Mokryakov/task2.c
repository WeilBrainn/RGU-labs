#include <stdio.h>

int main()
{
  int a, k;

  // Ввод числа от пользователя
  printf("Введите число a: ");
  scanf("%d", &a);

  // Ввод номера бита от пользователя
  printf("Введите номер бита k: ");
  scanf("%d", &k);

  // Проверка, чтобы номер бита был валидным
  if (k < 0 || k >= sizeof(int) * 8)
  {
    printf("Номер бита k должен быть в пределах от 0 до %d.\n", sizeof(int) * 8 - 1);
    return 1; // Завершение программы с ошибкой
  }

  // Вычисление k-ого бита
  int bit_k = (a >> k) & 1; // Сдвигаем a вправо на k и получаем младший бит

  // Вывод k-ого бита
  printf("К-ый бит числа %d (k = %d): %d\n", a, k, bit_k);

  return 0;
}
